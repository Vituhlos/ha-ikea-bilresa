# v0.6.0 release-readiness checklist

Working checklist for cutting a stable **v0.6.0**. `PROJECT_STATUS.md` remains the
canonical, dated validation record; this file only orders the outstanding work and
marks what blocks the stable tag. Keep the validation vocabulary
(Implemented / Static / Unit / CI / Hardware / Released) — never infer Hardware
from CI.

**Current state (2026-07-18):** highest tag is the prerelease `v0.6.0-rc.6`
(commit `dd14521`, CI green, deployed). No stable `v0.6.0`. `origin/main` is still
at `f1e7583`; PR #2 (`agent/dual-button-0.6`) and PR #1 (`agent/stabilize-0.5-x`)
are both open. The BILRESA **dual button (E2489)** feature (B0–B4) is implemented
and Hardware-verified; the remaining work is mostly scroll-wheel polish plus
release mechanics.

Legend: 🔴 blocks stable · 🟡 investigate (owner's call) · 🟢 deferrable ·
(HW) needs the physical device (owner) · (code) implementable without hardware.

## 🔴 Blockers — required before tagging a stable v0.6.0

- [ ] **1. Scroll smoothing on large cumulative batches** (code done, **HW A/B
  outstanding**). Mechanism implemented 2026-07-29: a single notch (eager first
  notch, hold-to-ramp step) is never smoothed, and a batch is spread in
  proportion to its size up to the binding's configured value. The `Transition`
  field became `Smoothing of large jumps` and is that ceiling, so the A/B runs
  from the UI without a new release. **The duration itself is still unmeasured**
  — per `PROJECT_STATUS.md` no value is accepted without a controlled Hardware
  A/B, so this item stays open until one is recorded.
  The second half of the original symptom — the light looking stationary at
  min/max — is **not** a smoothing defect and is deliberately not addressed
  here: the suppressed service calls are correct, since the target really is at
  its limit. If it needs anything, it needs feedback elsewhere in the UI.
- [x] **2. Hardware-verify the target-preservation fix** (HW). **Done
  2026-07-29 on deployed rc.10.** The RC.6 form of this fix turned out to be
  insufficient and was superseded twice — see #6 — so what is confirmed here is
  the final trajectory-based version: across a 74-step session on the physical
  wheel, **73 steps continued the calculated target** and the only discarded
  target was a correct one. Captures are committed under
  `tests/fixtures/captures/`.
- [ ] **3. Close Section G (physical scroll optimization)** (HW).
  Recorded as in progress with no G Hardware box claimed yet.
- [ ] **4. B3 panel — real Home Assistant pass** (HW/owner). **Partly done
  2026-07-29 on deployed rc.7: light theme reviewed from the owner's real-HA
  screenshots.** Confirmed in the real frontend: the regrouped editor renders as
  designed (Rotation / Button sections, one row per gesture, double and triple
  press full-width, minimum and maximum brightness on one row inside a collapsed
  Advanced), and the rotation ledger row reads `Jas · …` rather than the old
  infinitive. **Still owed: dark theme, one custom theme, and the keyboard +
  screen-reader pass**, plus the dual-button view specifically — the screenshots
  cover a wheel channel.
- [ ] **5. Release mechanics** (owner + agent).
  Cut a **stable** (non-prerelease) tag from a CI-green commit and merge the PR
  chain: PR #2 → `agent/stabilize-0.5-x`, then PR #1 → `main`. Today `main` has
  none of this. **Directly wanted by a real external user** — see issue #3
  (double-press in the UI): that feature already exists in the pre-releases and
  only a stable release ships it to users on the default channel (latest stable is
  `v0.5.0`, which predates it).

## 🟡 Investigate — quality, may or may not block

- [x] **6. RC.5 brightness accounting anomaly** (code/analysis). **Real bug,
  fixed and Hardware-confirmed on rc.10, 2026-07-29.** Not clamping and not
  rounding. It took three attempts, and the first two were wrong about the
  cause:
  1. rc.7 kept the target authoritative across the gap between two Matter
     gestures — necessary, but not sufficient;
  2. rc.9 replaced value-matching with a **trajectory** test after a hardware
     capture showed the dimmer reporting values it had never been sent (71, 76,
     135, 148), one rebase discarding nine notches at once;
  3. rc.10 coalesces command bursts.

  Confirmed on the wheel: 73 of 74 steps continue the calculated target, one
  correct discard, zero mistaken ones. Full diagnosis in `PROJECT_STATUS.md`;
  the captures are committed as regression tests.
- [x] **7. Verify the dual-button icon renders** (owner, visual). **Done
  2026-07-29 on deployed rc.7.** The owner's overview screenshot shows
  `Tlačítko Obývák` carrying a glyph distinct from both wheels, so
  `bilresa:dual-button` resolves through the custom-icon provider rather than
  falling back.

## 🟢 Deferrable — fine to leave until after v0.6.0

- [ ] **8. Legacy `ikea_bilresa` identifier registry migration.**
  Recorded as "not needed, separate owed item" — only if it turns up in the field.
- [ ] **9. Remaining 0.5.x wheel Hardware gates** (0.5.1–0.5.7 "Hardware pending").
  The runtime is already in rc.6; close them formally only if desired.

## Critical path

**#1 (smoothing) → #2 / #3 (hardware confirmation) → #4 (panel) → #5 (merge +
stable tag).** Handle #6 / #7 alongside; keep #8 / #9 below the line.
